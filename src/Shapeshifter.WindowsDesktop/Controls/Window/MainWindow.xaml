<Window x:Class="Shapeshifter.WindowsDesktop.Controls.Window.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:window="clr-namespace:Shapeshifter.WindowsDesktop.Controls.Window"
        xmlns:viewModels="clr-namespace:Shapeshifter.WindowsDesktop.Controls.Window.ViewModels"
        d:DataContext="{d:DesignInstance Type=viewModels:MainViewModel}"
        Top="{Binding ActiveScreen.WorkingArea.Y, Mode=OneWay}"
        Left="{Binding ActiveScreen.WorkingArea.X, Mode=OneWay}"
        Width="{Binding ActiveScreen.WorkingArea.Width, Mode=OneWay}"
        Height="{Binding ActiveScreen.WorkingArea.Height, Mode=OneWay}"
        Topmost="True"
        mc:Ignorable="d"
        ShowInTaskbar="False"
        SnapsToDevicePixels="True"
        UseLayoutRounding="True"
        ResizeMode="NoResize"
        Title=""
        WindowStyle="None"
        Background="#01000000"
	IsHitTestVisible="True"
        AllowsTransparency="True">
    <window:UserInterfaceControl x:Name="UserInterfaceControl" />
</Window>