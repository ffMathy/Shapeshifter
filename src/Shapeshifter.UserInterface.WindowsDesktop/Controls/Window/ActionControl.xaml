<UserControl x:Class="Shapeshifter.UserInterface.WindowsDesktop.Controls.Window.ActionControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:local="clr-namespace:Shapeshifter.UserInterface.WindowsDesktop.Controls.Clipboard"
             mc:Ignorable="d" Style="{StaticResource ActionControlContentAreaStyle}">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition />
            <RowDefinition />
        </Grid.RowDefinitions>
        <TextBlock Style="{StaticResource ActionControlHeaderStyle}" Text="{Binding Title}" />
        <TextBlock Grid.Row="1" Style="{StaticResource ActionControlContentStyle}" Text="{Binding Description}" />
    </Grid>
</UserControl>